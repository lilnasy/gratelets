From 3df3ec5c8e091a72882811e17e915223c61034d6 Mon Sep 17 00:00:00 2001
From: Arsh <69170106+lilnasy@users.noreply.github.com>
Date: Sun, 16 Mar 2025 21:27:32 -0400
Subject: [PATCH 2/3] use implementation

---
 src/index.ts  | 76 ++++++++++++++++++++++++++++++++++++++++++++++++++-
 src/server.ts | 16 ++++++++++-
 2 files changed, 90 insertions(+), 2 deletions(-)

diff --git a/src/index.ts b/src/index.ts
index 10726a3..9ac8942 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -3,6 +3,54 @@ import { join, relative } from "node:path";
 import { fileURLToPath } from "node:url";
 import type { BuildConfig, InternalOptions, Options } from "./types.ts";
 import { createConfigPlugin } from "./vite-plugin-config.ts";
+import type { ViteDevServer } from "./websocket/dev-middleware.js";
+
+export interface Locals {
+    /**
+     * Whether the current request wants the connection to be upgraded
+     * to a WebSocket.
+     */
+    isUpgradeRequest: boolean
+    /**
+     * Upgrade an incoming HTTP request to a bidirectional WebSocket
+     * connection.
+     *
+     * Returns a pair of {@linkcode WebSocket} and {@linkcode Response}
+     * instances. The request must be responded to with the provided
+     * response for the provided WebSocket to open and start receiving
+     * messages from the browser.
+     *
+     * ```ts
+     * export const GET: APIRoute = ctx => {
+     *     if (ctx.locals.isUpgradeRequest) {
+     *         const { response, socket } = ctx.locals.upgradeWebSocket()
+     *         socket.onmessage = event => {
+     *             if (event.data === "ping") {
+     *                 socket.send("pong")
+     *             }
+     *         }
+     *         return response
+     *     }
+     *     return new Response("Upgrade required", { status: 426 })
+     * }
+     * ```
+     *
+     * Calling this function on its own does not connect the WebSocket.
+     * It only returns a pair of objects that will establish the connection
+     * once the generated response has been returned from the API Route.
+     *
+     * Throws if the request is not an upgrade request.
+     */
+    upgradeWebSocket(): { socket: WebSocket, response: Response };
+}
+
+interface NodeLocals extends Locals {}
+
+declare global {
+    namespace App {
+        export interface Locals extends NodeLocals {}
+    }
+}

 // REF: https://github.com/denoland/deno/tree/main/ext/node/polyfills
 const COMPATIBLE_NODE_MODULES = [
@@ -65,16 +113,33 @@ export const JSR_STD_PATH = "jsr:@std/path@^1.1.4";
 
 export default function createIntegration(args?: Options): AstroIntegration {
   let _buildConfig: BuildConfig;
+  let viteDevServer: ViteDevServer;
   const internalOptions: InternalOptions = { ...args, relativeClientPath: "" };
   return {
     name: "@deno/astro-adapter",
     hooks: {
-      "astro:config:setup": ({ updateConfig }) => {
+      "astro:config:setup": ({ addMiddleware, command, updateConfig }) => {
         updateConfig({
           vite: {
+            esbuild: {
+              tsconfigRaw: {
+                compilerOptions: {
+                  useDefineForClassFields: true,
+                },
+              },
+            },
             plugins: [createConfigPlugin(internalOptions)],
           },
         });
+        if (command === "dev") {
+          addMiddleware({
+            order: "pre",
+            entrypoint: new URL(
+              "./websocket/dev-middleware.ts",
+              import.meta.url,
+            ),
+          });
+        }
       },
       "astro:config:done": ({ setAdapter, config }) => {
         const clientPath = join(fileURLToPath(config.build.client));
@@ -97,6 +162,15 @@ export default function createIntegration(args?: Options): AstroIntegration {
         });
         _buildConfig = config.build;
       },
+      "astro:server:setup"({ server }) {
+        viteDevServer = server;
+      },
+      async "astro:server:start"() {
+        const { handleUpgradeRequests } = await import(
+          "./websocket/dev-middleware.js"
+        );
+        handleUpgradeRequests(viteDevServer);
+      },
       "astro:build:setup": ({ vite, target }) => {
         if (target === "server") {
           vite.resolve = vite.resolve ?? {};
diff --git a/src/server.ts b/src/server.ts
index 4eb59bb..9f7c415 100644
--- a/src/server.ts
+++ b/src/server.ts
@@ -49,7 +49,21 @@ async function start() {
       span?.setAttribute("astro.type", routeData.type);
       const hostname = handlerInfo.remoteAddr?.hostname;
       Reflect.set(request, Symbol.for("astro.clientAddress"), hostname);
-      const response = await app.render(request, { routeData });
+      const response = await app.render(request, {
+        routeData,
+        locals: {
+          get isUpgradeRequest() {
+            return request.headers.get("upgrade") === "websocket"
+          },
+          upgradeWebSocket() {
+            // @ts-ignore
+            const { socket, response } = Deno.upgradeWebSocket(request);
+            // match the browser's default binaryType
+            socket.binaryType = "blob";
+            return { socket, response };
+          }
+        }
+      });
       if (app.setCookieHeaders) {
         for (const setCookieHeader of app.setCookieHeaders(response)) {
           response.headers.append("Set-Cookie", setCookieHeader);
-- 
2.43.0
